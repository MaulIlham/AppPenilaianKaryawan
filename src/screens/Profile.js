import React from 'react';
import {View, Text} from 'react-native';

const Profile = props => {
    return(
        <View style={{justifyContent: 'center', alignItems: 'center'}}>
            <Text style={{fontSize: 40}}>Welcome To Profile</Text>
        </View>
    );
}

export default Profile;
